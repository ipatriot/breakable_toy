<h1> Problems </h1>
<ul>
<% @problems.each do |problem| %>
    <li><%=link_to problem.name, problem_path(problem.id) %></li>
 <%= problem.address %>||<%= problem.latitude %> <%= problem.longitude%> </li>
<% end %>
</ul>

<a href = "/problems/new">Create a new problem</a>

insert a map with all the markers!!

<script src="http://maps.googleapis.com/maps/api/js?ENV[GOOGLE_MAPS_KEY]"></script>

<div class="the_map" id="googleMap" style="width:800px;height:520px;"></div>
